{
  "name": "twig-pattern-library",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node index.js",
    "serve": "browser-sync start --server dist/ --directory ",
    "watch:src": "nodemon --watch src/ --ext twig,json --exec 'npm run build' ",
    "start": "npm run build && (npm run serve & npm run watch:src)"
  },
  "dependencies": {
    "glob": "^7.0.3",
    "twig": "^0.8.8",
    "fs-extra": "^0.26.5"
  },
  "devDependencies": {
    "browser-sync": "^2.11.1",
    "nodemon": "^1.9.1"
  },
  "author": "",
  "license": "ISC"
}
